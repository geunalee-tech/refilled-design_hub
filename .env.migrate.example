# 데이터 이관용 환경변수 — 이 파일을 .env.migrate로 복사해 값을 채우세요 (.env.migrate는 gitignore 등록됨)
# Supabase 대시보드 → Settings → API 에서 확인

# 프로젝트 URL (https://{ref}.supabase.co)
SUPABASE_URL=

# secret(service_role) 키 — RLS 우회 관리자 키. 절대 커밋·공유 금지
SUPABASE_SERVICE_KEY=
